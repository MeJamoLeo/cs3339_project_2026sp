# Ignore everything
*

# Allow directories (so nested files can be tracked)
!*/

# Allow these file types
!*.lisp
!*.png
!*.tex
!README.md

# Allow .gitignore itself
!.gitignore
!input*

# Allow Nix flake
!flake.nix
!flake.lock
